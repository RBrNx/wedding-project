mutation FETCH_TEMP_LOGIN_CREDENTIALS_MUTATION($input: FetchCredentialsInput!) {
  fetchTempLoginCredentials(input: $input) {
    success
    message
    payload {
      username
      password
    }
  }
}
