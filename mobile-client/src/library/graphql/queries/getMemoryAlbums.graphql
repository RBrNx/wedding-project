query getMemoryAlbums($filter: MemoryFilterInput!) {
  getMemoryAlbums(filter: $filter) {
    images {
      ... on ImageMemory {
        _id
        photoId
        eventId
        url
        thumbnail
        createdAt
        updatedAt
      }
    }
  }
}
